<div class="container intro">
	<h1>Welcome to Rock Paper Scissors</h1>
	<p>This application makes use of ActionCable to provide a two-player environment for playing this classic game. Please login or register below to get started.</p>
</div>

<div class="row">
	<div class="col-md-4"></div>
	<div class="col-md-4">
		<% if current_user %>
			<%= link_to "New Game", :new_game, class: "btn btn-primary btn-block" %>
		<% else %>
			<%= link_to "Login", :new_user_session, class: "btn btn-primary btn-block" %>
			<%= link_to "Register", :new_user_registration, class: "btn btn-primary btn-block" %>
		<% end %>
	</div>
	<div class="col-md-4"></div>
</div>

<div class="container intro">
	<h1>Join a game!</h1>
	<p>Any user-created games will be shown below. Join one for a challenge!</p>
</div>

<div class="container">
	<% @games.each do |game| %>
		<li><%= link_to(game.id.to_s, edit_game_path(game.id)) %></li>
	<% end %>
</div>